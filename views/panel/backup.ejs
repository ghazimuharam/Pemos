<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <% for (var i = 0; i < users.length; i++) { %>
                            <h3><%= users[i].UniqueCode %></h3>
                            <p><%= users[i].Type %></p>
                            <p>$<%= users[i].Vote %></p>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>